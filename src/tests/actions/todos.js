import configureMockStore from 'redux-mock-store'
import thunk from 'redux-thunk'
import {  } from '../../actions/toDoActions'

const createMockStore = configureMockStore([thunk])

test('should add a todo to database', () => {
    
})